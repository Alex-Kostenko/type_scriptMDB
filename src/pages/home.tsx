import React from 'react';
import styled from 'styled-components';


const Home: React.FC = ( ) => {

  return (
    <div> 0 </div>
  );
}

const StyledSection = styled.div`
  display: flex;
`;

export default Home;
